server:
  port: 8081

spring:
  application:
    name: inmobiliaria-service

  datasource:
    # Colocamos la URL pública de tu Railway con los parámetros de red obligatorios directos aquí
    url: jdbc:mysql://reseau.proxy.rlwy.net:44401/railway?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: bPmsKrChvLxjHkGTUADKbHaXOjcVHGAS
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  liquibase:
    enabled: false

  cloud:
    compatibility-verifier:
      enabled: false

  springdoc:
    swagger-ui:
      path: /swagger-ui.html
    api-docs:
      path: /v3/api-docs

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: false